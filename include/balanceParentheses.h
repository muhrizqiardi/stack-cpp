#include <include/stack.h>
#include <iostream>

Stack parStack;

// Returns true if it's balanced, otherwise false
bool check(char arr[], int arrLength)
{
    for (int i = 0; i < arrLength; i++)
    {

        switch (arr[])
        {
        case /* constant-expression */:
            /* code */
            break;

        default:
            break;
        }
    }
}
